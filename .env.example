# Server-side ONLY. Never commit real values, never expose to the client.

# --- OpenAI (story/scene/prompt reasoning) ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-mini

# --- KIE.ai (image rendering) ---
KIE_API_KEY=
KIE_WEBHOOK_HMAC_KEY=
KIE_WEBHOOK_SECRET=

# --- Public base URL (used to build KIE callBackUrl + Vercel cron) ---
APP_BASE_URL=http://localhost:3000

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# --- Database (Supabase Postgres connection string) ---
DATABASE_URL=

# --- Reconciliation sweep secret (protects /api/cron/reconcile) ---
CRON_SECRET=
